TraceWeaver 프로젝트 이어서 진행해줘.

직전 세션에서 B0 Foundation 전체 + UI shell 7 screens + B1-1 shell collector
+ B5-1 demo seed 까지 land 했고, end-to-end 검증 (데몬 부팅 → demo seed →
shell hook fire → events/conventions/projects 읽기 → patch → SPA 직접 서빙)
모두 통과했어.

다음 세션 첫 액션:

1. 먼저 `docs/handoff/2026-04-26_session-handoff.md` 정독해줘. 거기 §0~§8 에
   30초 요약 / 동작 확인된 것 / 알려진 stub / 알려진 마이너 버그 / 다음 세션
   우선순위 / 핵심 파일 인덱스 / 운영 룰 / 5분 검증 체크리스트 다 들어 있어.

2. `docs/plan/16_roadmap.md` 의 B1 ~ B3 task 도 같이 읽어둬. 1차 진리는 여전히
   `docs/plan/` 19개 문서임 (15 ADR 잠금). CLAUDE.md 의 "Rust/Tauri/Phi-4" 표기는
   stale — 무시.

3. 핸드오프 §7 의 4단계 검증 체크리스트 (uv venv / 19 module import / 데몬
   부팅 + demo seed / vite build) 부터 돌려서 환경이 그대로 유효한지 확인.

4. 그 다음 §4 P0 3건 (status 라우터 라이브 wire / pyright --strict 전체 /
   pytest 그린) 마무리.

5. §4 P1 중에서 골라 진입. 추천 우선순위:
   (a) WebSocket broadcaster 를 ShellCollector / PATCH conventions / demo seed
       에 wire 한 다음 ui/src/main.tsx 에 ws subscribe + TanStack Query
       invalidate 도입 → Today / Inbox 가 라이브로 갱신되는 모습이 데모에서
       가장 임팩트 있음.
   (b) B1-2 git collector (Dulwich + post-* hooks 자동 인스톨러).
   (c) `traceweaver.store.repository` 의 미완 메서드 채우면서 라우터 thin refactor.

운영 룰 (지난 세션과 동일하게 유지):
- Codex pair review 는 SKIP. 내가 명시적으로 "코덱스 켜라" 하기 전까진 그대로.
- UI 는 니가 직접 작성. "클로드 디자인" 위임 없음.
- 모든 변경은 docs/work_log/<topic-slug>/<YYYY-MM-DD>_<action>.md 5섹션 형식
  (Context / Files changed / Why / Verification / Follow-ups). 트리비얼해도
  10줄짜리 로그 남겨.
- `docs/plan/` 가 1차 진리. 충돌 시 plan 이 이김. PyPI 현실 보정 (apscheduler
  3.x 처럼) 은 preset audit log 에 사유 명시.
- 에이전트팀 구동해서 철저하개 분석 검토하면서 진행해

ollama pull qwen2.5-coder:7b-instruct-q4_K_M
hf download Qwen/Qwen2.5-Coder-7B-Instruct

위 두개는 이미 실행해서 다운 받아놨어

철저하게 분석 검토하면서 진행해줘.
